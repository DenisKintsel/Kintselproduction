<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.login">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permissi on.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".VxodActivity"></activity>
        <activity android:name=".PKGryzActivity" />
        <activity android:name=".PKDriverActivity" />
        <activity
            android:name=".MyADSActivity"
            android:label="Мои объявления" />
        <activity android:name=".Detail3Activity" />
        <activity
            android:name=".ProfilActivity"
            android:label="Профиль" />
        <activity android:name=".DetailActivity" />
        <activity android:name=".Detail2Activity" />
        <activity android:name=".BDdriver" />
        <activity android:name=".PoiskActivity" />
        <activity android:name=".DriverActivity" />
        <activity android:name=".GryzActivity" />
        <activity android:name=".OpenProfil" />
        <activity android:name=".Splashscreen"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity
            android:name=".Login22"
            android:label="@string/app_name" />
        <activity
            android:name=".LogginActivity"
            android:theme="@style/AppTheme" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>